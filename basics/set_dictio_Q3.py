#figure out the way to store 9 and 9.0 as seprate values in the set
values={"9.0",9}
print(values)

#or next method is to store like values={("float",9.0),9} but values={"9.0",9 will still give the same value}